# Paper-ready preset: 30 epochs, patience=10, 3 seeds, CUDA.
# Pair with experiment=full for the final publication benchmark run.
epochs: 30
batch_size: 32
patience: 10
lr: 1.0e-4
seeds: [42, 123, 2026]
device: cuda
m4_seq_len: 96
dry_run: false
