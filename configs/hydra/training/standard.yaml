# Standard benchmark preset: 20 epochs, 3 seeds, CUDA.
# Pair with experiment=long_term, experiment=m4, or experiment=full.
epochs: 50
batch_size: 32
patience: 5
lr: 1.0e-4
seeds: [42, 123, 2026]
device: cuda
m4_seq_len: 96
dry_run: false
